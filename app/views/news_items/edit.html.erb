<%= govuk_breadcrumbs 'News': news_items_path %>

<h2>Edit News story</h2>

<%= render 'form', news_item: @news_item, collections: @collections %>

<% if policy(@news_item).destroy? %>
  <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">

  <h3>
    Remove News item
  </h3>

  <p>
    Remove this News item
  </p>

  <%= button_to 'Remove',
                news_item_path(@news_item),
                method: :delete,
                class: 'govuk-button govuk-button--warning',
                data: { turbo_confirm: "Are you sure you want to remove this news item?", turbo_submits_with: "Working..." }
  %>
<% end %>
